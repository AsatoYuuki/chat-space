.side-chat__left
  .left-chat__user-name
    = current_user.name
  %ul
    %li.edit
      = link_to new_group_path do
        =fa_icon 'edit', class: 'fas'
    %li.cog
      = link_to edit_user_path(current_user) do
        =fa_icon 'cog', class: 'fas'
.groups
  - current_user.groups.each do |group|
    = link_to group_messages_path(group) do
      .group
        .group__name
          = group.name
        .group__message
          メッセージはまだありません。
      